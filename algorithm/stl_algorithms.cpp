#include<bits/stdc++.h>
using namespace std;


/*

WAY TO SORT A ARRAY 😀

int main(){
    int arr[6] = {8,7,6,5,4,3};
    int size = sizeof(arr)/sizeof(arr[0]);
    int work = size/2;
    // sort(arr, arr+work);
    // for(auto it : arr){
    //     cout << it << " ";
    // }
    // cout << endl;
    sort(arr+work, arr+size);
    for(auto it : arr){
        cout << it << " ";
    }
}

*/


/*

WAY TO SORT A VECTOR 😁
int main(){
    vector<int> v;
    v = {8,7,6,5,4,3};
    sort(v.begin(), v.end());

    for(auto it : v){
        cout << it << " ";
    }
}

*/

/*
//SORT A VECTOR IN DESCENDING ORDER
int main(){
    vector<int> v;
    v = {8,10,2,3,6,4,100,99};
    sort(v.begin(), v.end(), greater<int>());

    for(auto it : v){
        cout << it << " ";
    }
}

*/


/*
int main(){
    string s = "3476";
    sort(s.begin(), s.end());
    cout << s << endl;

    char max = *max_element(s.begin(),s.end());
    cout << max;
}
*/




